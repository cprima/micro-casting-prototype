[project]
name = "crawling"
version = "0.0.1"
description = "Shared Crawl4AI client"
requires-python = ">=3.10"
dependencies = ["crawl4ai==0.7.6"]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/crawling"]
