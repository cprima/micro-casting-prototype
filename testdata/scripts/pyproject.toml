[project]
name = "mcp-testdata-generators"
version = "0.1.0"
description = "Test data generation scripts for MCP server blueprints"
requires-python = ">=3.10"
dependencies = [
    "faker>=22.0.0",
    "python-dateutil>=2.8.2",
]
