[project]
name = "mcp-json-retrieval"
version = "0.1.0"
description = "JSON document querying"
requires-python = ">=3.10"
dependencies = ['mcp>=0.1.0', 'jsonpath-ng>=1.6.0']

[tool.uv]
dev-dependencies = [
    "pytest>=7.0.0",
    "pytest-asyncio>=0.21.0",
    "pytest-cov>=4.0.0",
]
