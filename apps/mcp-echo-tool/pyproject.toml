[project]
name = "mcp-echo-tool"
version = "0.1.0"
description = "Simple tool invocation pattern"
requires-python = ">=3.10"
dependencies = ['mcp>=0.1.0']


[tool.uv]
dev-dependencies = [
    "pytest>=7.0.0",
    "pytest-asyncio>=0.21.0",
    "pytest-cov>=4.0.0",
]
