[project]
name = "mcp-http-retrieval"
version = "0.1.0"
description = "REST API data fetching"
requires-python = ">=3.10"
dependencies = ['mcp>=0.1.0', 'httpx>=0.25.0']


[tool.uv]
dev-dependencies = [
    "pytest>=7.0.0",
    "pytest-asyncio>=0.21.0",
    "pytest-cov>=4.0.0",
]
