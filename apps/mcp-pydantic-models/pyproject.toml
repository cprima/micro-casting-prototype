[project]
name = "mcp-pydantic-models"
version = "0.1.0"
description = "Type-safe serialization with Pydantic"
requires-python = ">=3.10"
dependencies = ['mcp>=0.1.0', 'pydantic>=2.0.0']


[tool.uv]
dev-dependencies = [
    "pytest>=7.0.0",
    "pytest-asyncio>=0.21.0",
    "pytest-cov>=4.0.0",
]
